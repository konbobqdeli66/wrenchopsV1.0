# Copy-paste template for PRODUCTION.
#
# Usage on the droplet:
# 1) Copy this file to backend/.env
# 2) Replace all values marked as CHANGE_ME / YOUR_*
#
# NOTE: backend/.env is ignored by git and must NOT be committed.

# --- Server ---
NODE_ENV=production
PORT=5000

# --- Security (REQUIRED) ---
# Change this to a long random string (example: 64+ characters)
JWT_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_SECRET

# --- Bootstrap admin (recommended on a fresh install) ---
# If the DB has no admins, the backend will create one on startup.
BOOTSTRAP_ADMIN_NICKNAME=admin
BOOTSTRAP_ADMIN_PASSWORD=CHANGE_ME_ADMIN_PASSWORD

# --- SMTP (optional, but REQUIRED if you will use invitations/password reset/invoice emails) ---
# For Gmail you typically need an "App Password" (not your normal password).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=YOUR_EMAIL@gmail.com
SMTP_PASS=YOUR_APP_PASSWORD
SMTP_FROM="WrenchOps <YOUR_EMAIL@gmail.com>"

