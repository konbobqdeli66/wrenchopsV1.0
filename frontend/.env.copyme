# Optional production env for the React build.
#
# In the recommended Nginx reverse-proxy setup, you can also omit this entirely,
# because the frontend defaults to same-origin.

# If you still want to pin it explicitly:
REACT_APP_API_BASE_URL=https://viatransport-service.xyz

